name: fast_noise
description: A starting point for Dart libraries or applications.
version: 0.0.1
#homepage: https://www.example.com
#author: Frank <email@example.com>

environment:
  sdk: '>=1.20.1 <2.0.0'

dependencies:
  fixnum: ^0.10.0

dev_dependencies:
  test: ^0.12.0
  benchmark_harness: ^1.0.0
